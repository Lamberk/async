[project]
name = "async"
version = "0.1.0"
description = "This is part of async course from dvmn.org"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "ipython>=8.37.0",
    "ruff>=0.14.4",
]
